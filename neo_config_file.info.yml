name: 'Neo | Config File'
type: module
description: 'Provides the ability to store files in config.'
package: Neo
version: 1.0.25
core_version_requirement: ^10.3 || ^11
container_rebuild_required: true
dependencies:
  - drupal:file
  - neo:neo
configure: entity.neo_config_file.collection
